# ================== CONFIGURATION PHANTOM ==================
#
# provider: anthropic | openai | grok | gemini | ollama | mistral | deepseek | llamacpp
provider: "anthropic"

# model: leave empty to use the provider's default
#   anthropic -> claude-sonnet-4-6
#   openai    -> gpt-5.4
#   grok      -> grok-4-20-beta
#   gemini    -> gemini-3.0-pro
#   ollama    -> deepseek-v3.2:cloud  (local)
#   mistral   -> mistral-large-latest
#   deepseek  -> deepseek-chat-v3.2
#   llamacpp  -> (auto-detect from server)
model: "claude-sonnet-4-6"

# Authentication method: "api_key" (default) or "oauth"
auth_method: "oauth"

# API key -- set via environment variable or generated by install.sh / install.ps1
api_key: ""

# OAuth settings (only used when auth_method: "oauth")
# Set tokens via environment variables (recommended) or here directly:
#   ANTHROPIC_OAUTH_ACCESS_TOKEN, ANTHROPIC_OAUTH_REFRESH_TOKEN,
#   ANTHROPIC_OAUTH_CLIENT_ID, ANTHROPIC_OAUTH_CLIENT_SECRET
# oauth_access_token: ""
# oauth_refresh_token: ""
# oauth_client_id: ""
# oauth_client_secret: ""
# oauth_token_url: "https://auth.anthropic.com/oauth/token"

# llama.cpp OpenAI-compatible API (used when provider: "llamacpp")
llamacpp_base_url: "http://192.168.91.1:8080/v1"

# Ollama only
ollama_host: "http://localhost:11434"

# Paths
log_dir: "logs"
scope_file: "scopes/current_scope.md"

# Behaviour
debug: false
autonomous: true
max_autonomous_turns: 50
pause_every_n_turns: 10

# Interactive mode
interactive: true

# Timeouts (seconds)
sqlmap_timeout: 300
nmap_timeout: 300

# Ollama LLM timeout (seconds) -- local models may need more time
ollama_timeout: 300

# Metasploit timeout (seconds) -- exploits may need time to run
metasploit_timeout: 120

# Parallel tool execution
max_parallel_tools: 4

# Rate limiting for HTTP requests (requests/sec)
requests_per_second: 5

# Retry config for HTTP tools (recon, payloads)
retry_max: 3
retry_backoff: 2

# Context compaction -- tool results older than this many turns are truncated
context_compact_after: 5

# Stall detection -- inject pivot guidance after N turns with no new findings
stall_threshold: 5

# Stealth profile: silent | stealthy | normal | aggressive
stealth_profile: "normal"

# HTTP proxy for all tool requests (optional, e.g. Burp Suite)
# proxy: "http://127.0.0.1:8080"
